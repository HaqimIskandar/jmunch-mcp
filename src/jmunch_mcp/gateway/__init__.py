"""jmunch v2 HTTP gateway — OpenAI/Anthropic-compatible proxy frontend.

Reuses the jmunch-mcp core (sniffer, registry, verbs, meta, metrics) unchanged.
Ship as `pip install jmunch-mcp[gateway]` (aiohttp is the only runtime dep).
"""
